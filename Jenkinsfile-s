node("workstation"){
  try {
      stage("one") {
         echo "one"
         exit 1
      }

      stage("two") {
           echo "two"
      }
    }catch (e){
      stage("Declarative post actions") {
        echo "This will run when itz a failure"
    }
  }
}